#ifndef _parsechanges_H
#define _parsechanges_H 1
#include <grace/application.h>

//  -------------------------------------------------------------------------
/// Implementation template for application config.
//  -------------------------------------------------------------------------
typedef configdb<class parsechangesApp> appconfig;

//  -------------------------------------------------------------------------
/// Main application class.
//  -------------------------------------------------------------------------
class parsechangesApp : public application
{
public:
		 		 parsechangesApp (void) :
					application ("nl.madscience.site.parsechanges")
				 {
				 	opt = $("-h", $("long", "--help"));
				 }
				~parsechangesApp (void)
				 {
				 }
	
	int			 main (void);

protected:
	appconfig	 conf;
};

#endif

